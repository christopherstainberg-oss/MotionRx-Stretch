"use client";

import { GlobalSearch } from "@/components/GlobalSearch";

/** Client search island for the server-rendered home page */
export function HomeSearch() {
  return <GlobalSearch variant="hero" />;
}
